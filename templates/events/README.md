# Event Templates

These are example snippets only. They are not loaded by the website and will not render.

Copy one template object and paste it into the `items` array in:

`assets/data/events.json`
